# BandProject 工作进度

## 2026-09-18：浑浊录音转写改进

- 已保留上一阶段的节拍、调性与和弦可信度门槛优化（工作区中已有未提交改动）。
- 本阶段为 Basic Pitch 增加模型专用输入：保留原始的立体声归一化母带，同时生成单声道的轻度去噪、带通与动态归一化版本供音高识别使用。
- 浏览器会将远端转写任务 ID 保存在本地存储中。网络暂断、轮询超时、页面刷新或浏览器重启后，重新选择同一文件会继续查询原任务；仅当服务明确重启并丢失任务时才重新上传。
- 已验证：服务端 20 项 Python 合约/分析测试均通过，且 `git diff --check` 无格式错误。
- 待验证：在部署服务上用用户的浑浊录音做实际回归；当前工作区没有 Node 或 ffmpeg 可执行环境，前端静态检查与真实滤镜运行需要在部署/完整开发环境完成。

## 2026-09-18：浑浊录音双路识别

- 将单次增强识别升级为双路：Basic Pitch 分别分析轻度去噪音轨与原始归一化音轨。
- 同音高、相近起音或大幅重叠的事件会对齐融合；两路一致的弱音保留，只在单路出现的碎片必须满足更高的时长和置信度门槛。
- `auto`、钢琴和和弦配置降低起音/帧阈值以召回被浑浊背景遮盖的弱音，双路投票负责抑制由此增加的假音。
- 可选 ffmpeg 增强滤镜失败时改为单路回退，不再让整次扒谱失败。
- 已验证：24 项服务端测试通过，`git diff --check` 通过；工作区内没有可用于端到端复测的音频文件。
- 线上合成浑浊和弦冒烟测试发现 Basic Pitch 临时 MIDI 写盘会因重叠噪声事件报 `negative delta time`；已移除这条冗余导出路径，最终 MIDI/MusicXML 仍由融合、精修后的统一事件时间线生成。
- 复测确认异常实际发生在 Basic Pitch 公共 `predict()` 内部的 PrettyMIDI 构造阶段；现改为调用其官方推理与音符解码函数、直接取得事件，并在模型窗口边界丢弃极少数结束时间不大于开始时间的非法事件。
